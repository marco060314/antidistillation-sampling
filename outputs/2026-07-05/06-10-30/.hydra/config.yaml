train_traces: gsm8k_repl/traces/train_poison007
do_eval: false
eval_epochs: 0.5
holdout_traces: gsm8k_repl/traces/holdout
max_length: 1536
student: meta-llama/Llama-3.2-3B
tokenizer: meta-llama/Llama-3.2-3B-Instruct
checkpoint: null
lora: true
lora_r: 128
lora_alpha: 128
lora_dropout: 0.0
seed: 1
wandb: false
repetition_penalty: 1.0
lr: 0.0005
weight_decay: 0.1
max_grad_norm: 1.0
warmup: 0.03
lr_scheduler_type: cosine
batch_size: 16
per_device_batch_size: 2
num_epochs: 3
exp_dir: gsm8k_repl
metadata_dir: ${exp_dir}/metadata
model_registry: ${metadata_dir}/model_registry.jsonl
model_dir: ${exp_dir}/models
model_name: gsm_poison_s1
model_path: ${model_dir}/${model_name}
